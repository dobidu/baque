---
phase: 01-setup
plan: 02
plan_type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [tests/CMakeLists.txt, tests/test_smoke.cpp, CMakeLists.txt, .github/workflows/ci.yml]
autonomous: false
description: "Catch2 test target + 3-OS CI (build, tests, pluginval, lint)"
type: Plan
about: "BAQUE"
---

<objective>
## Goal
Catch2 wired into CMake with a smoke test, and GitHub Actions CI building macOS (universal) + Windows x64 + Linux x64, running tests, pluginval, and format check on every push.

## Purpose
ESCOPO Fase 0 second half — "CI rodando" DoD. Every later phase merges against this gate. Linux is full v1 target (decision 2026-06-04), so CI matrix covers all three OSes from day one.

## Output
- tests/ target (Catch2 v3 via FetchContent) + smoke test
- .github/workflows/ci.yml: 3-OS matrix — build, ctest, pluginval, clang-format check
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/01-setup/01-01-SUMMARY.md (CMake targets + scripts/run_pluginval.sh this plan extends)
</context>

<acceptance_criteria>

## AC-1: Tests run locally
```gherkin
Given the project with tests target
When ctest --test-dir build is run
Then the Catch2 smoke test compiles, runs, and passes
```

## AC-2: CI green on all three OSes
```gherkin
Given the repo pushed to GitHub
When the CI workflow runs on macOS, Windows, and Linux
Then build, tests, pluginval, and format check all pass on every leg
```

## AC-3: macOS universal binary
```gherkin
Given the macOS CI leg
When the plugin is built
Then an explicit lipo -info CI step proves arm64 + x86_64 slices present (job fails otherwise)
```

## AC-4: AU validated on macOS <!-- audit-added -->
```gherkin
Given the macOS CI leg
When auval runs against the built AU component
Then validation passes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Catch2 target + smoke test</name>
  <files>tests/CMakeLists.txt, tests/test_smoke.cpp, CMakeLists.txt</files>
  <action>
    - Catch2 v3 via FetchContent (pinned tag); enable_testing() + add_subdirectory(tests) in root
    - tests/test_smoke.cpp: two cases —
      1. trivial sanity (framework wiring)
      2. instantiate PluginProcessor via createPluginFilter()/direct construction, prepareToPlay + processBlock on a silent buffer, assert no crash and output stays silent. Use juce::ScopedJuceInitialiser_GUI for message manager.
    - Keep DSP code testable without GUI (ESCOPO §8: DSP isolated from UI)
    Avoid: tests requiring audio device or display.
  </action>
  <verify>cmake --build build && ctest --test-dir build --output-on-failure exits 0</verify>
  <done>AC-1 satisfied: smoke test passes locally</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Matrix: macos-latest, windows-latest, ubuntu-latest. Per leg:
    - <!-- audit-added --> Top-level `permissions: contents: read` (least privilege) + `concurrency: cancel-in-progress` keyed on ref
    - Linux: apt deps (libasound2-dev, libx11-dev, libxext-dev, libxrandr-dev, libxinerama-dev, libxcursor-dev, libfreetype-dev, libfontconfig1-dev, libcurl4-openssl-dev, xvfb)
    - macOS: -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" (universal — AC-3)
    - Configure + build Release, run ctest <!-- audit-added: Linux ctest under xvfb-run — ScopedJuceInitialiser_GUI may touch X -->
    - pluginval: download per-OS release (pinned version, same as scripts/run_pluginval.sh), validate VST3 (Linux under xvfb-run)
    - <!-- audit-added --> macOS leg additionally validates AU: auval -v aumu Bqe1 Lvd0 (codes from 01-01) — otherwise AU untested until Phase 13
    - <!-- audit-added --> macOS leg asserts universal: lipo -info on plugin binary, fail unless both arm64 and x86_64 present
    - Lint job (ubuntu): clang-format --dry-run --Werror on src/ tests/
    - Cache JUCE/Catch2 FetchContent (~/.cache or build/_deps) keyed on CMakeLists hash
    Avoid: codesigning/notarization (Phase 13); release artifact upload (later).
  </action>
  <verify>yamllint-clean file; act dry-run optional; real verify in Task 3</verify>
  <done>Workflow file complete, covers AC-2 + AC-3 checks</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>CI workflow needs a GitHub remote to run.</what-built>
  <how-to-verify>
    1. Create GitHub repo (e.g. github.com/&lt;you&gt;/baque), public (open source GPLv3)
    2. git remote add origin &lt;url&gt; && git push -u origin main
    3. Watch Actions tab: all three legs green (build + tests + pluginval + lint)
  </how-to-verify>
  <resume-signal>Type "ci green" when all legs pass, or paste failing log</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/plugin_processor.* behavior (silence passthrough — established in 01-01)
- LICENSE, NOTICE (locked in 01-01)
- BAQUE-ESCOPO.md, BAQUE-UI-SPEC.md, BAQUE_MOCKUP.html

## SCOPE LIMITS
- No code signing / notarization (Phase 13)
- No release packaging or artifact publishing
- No clang-tidy CI job yet if runtime too slow — format check mandatory, tidy may be follow-up
- No golden-file audio regression yet (needs DSP first)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] ctest passes locally
- [ ] CI green on macOS + Windows + Linux (user-confirmed)
- [ ] macOS artifact universal (explicit lipo -info CI step, job-failing)
- [ ] auval passes on macOS AU <!-- audit-added -->
- [ ] pluginval passes in all three CI legs
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed including checkpoint
- Phase 1 DoD met: formats open in host, pluginval green, CI running
- ROADMAP Phase 1 can be marked Complete after UNIFY
</success_criteria>

<output>
After completion, create `.paul/phases/01-setup/01-02-SUMMARY.md`
</output>
