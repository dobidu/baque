# PAUL Handoff

**Date:** 2026-06-09 (session 23)
**Status:** paused — APPLY mid-checkpoint (Task 3 human-verify pending)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox audio plugin (VST3/AU/Standalone, JUCE 8 / C++17).
**Core value:** Producers build beats with authentic micro-timing feel — off-grid groove, lo-fi color, controlled error as first-class features.

---

## Current State

**Version:** 0.0.0
**Phase:** 9 of 13 — MIDI / Hardware (LAST plan of the phase)
**Plan:** 09-04 — TR-8/TR-8S mapping templates + Phase 9 DoD

**Loop Position:**
```
PLAN ──▶ AUDIT ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ◑        ○     [APPLY: Tasks 1&2 done; Task 3 checkpoint pending]
```

---

## What Was Done (this session)

- `/paul:plan 09-04` → standard plan: TR-8/TR-8S templates + Phase 9 DoD.
- `/paul:audit 09-04` → conditionally acceptable → upgraded. 1 must-have + 2 strong applied:
  - **M1**: Phase-9 DoD INT-lane check was vacuous → now requires non-zero audio (lane provably fired) before asserting no-INT-MIDI; DoD MUST drive `processBlock`, not `Sequencer::generate`.
  - **SR1**: `apply_template` non-destructive (set_note only); processor apply copies current pattern (no data loss); routing+cc reset documented intentional.
  - **SR2**: DoD asserts EXT note-off on gate close + EXACT 24ppqn clock count (not >0).
- `/paul:apply 09-04` — Tasks 1 & 2 COMPLETE and qualified:
  - **Task 1**: `src/audio/hardware_templates.h` — `HardwareTemplate` POD + `tr8_template()`/`tr8s_template()` + `apply_template()`. Note map = Roland GM ch10 (lanes 0-10: BD36 SD38 LT43 MT47 HT50 RS37 HC39 CH42 OH46 CC49 RC51). CC slots OFF by default (confirm on hardware). `BaqueProcessor::apply_hardware_template()` + `set_pattern()` added (plugin_processor.h/.cpp).
  - **Task 2**: `tests/test_phase9_dod.cpp` — P9D1..P9D6 via real `processBlock` + a `TestPlayHead`. All green.
  - **204/204 tests pass** (was 195 + 9 new); clang-format clean on all 5 changed files.

---

## What's In Progress

- **Task 3 (checkpoint:human-verify) — real TR-8/TR-8S sign-off — PENDING USER.**
  - Awaiting one of: `"approved"` (TR-8 plays in sync; optionally paste confirmed scatter CC numbers to bake into the template), or `"defer hardware"` (close Phase 9 on the software DoD, record hardware sign-off as outstanding), or issue report.
  - Software DoD is fully green; only the physical-hardware check remains (this dev box has no TR-8).

---

## What's Next

**Immediate:** Resolve Task 3 checkpoint (`approved` / `defer hardware`). Then APPLY finalizes → `/paul:unify .paul/phases/09-midi-hardware/09-04-PLAN.md`.

**After UNIFY:** Phase 9 complete → **phase-transition commit** (09-01..09-04 together on `main`, mirroring Phase 8 single-commit-at-transition). Then update PROJECT.md/ROADMAP/STATE to mark Phase 9 done; next is Phase 10 (UI/UX).

**Separate, time-critical:** CI actions/checkout+cache v4→v5 on branch `ci/node20-actions-v5`, PR #1 (https://github.com/dobidu/baque/pull/1) — **merge before June 16, 2026** (Node.js 20 deprecation).

---

## Deviations from Plan (for UNIFY)

- **SR1 accessor**: plan said add `Sequencer::current_pattern()`; an equivalent `pattern() const` already existed → reused it instead of adding a redundant method (no new accessor needed).
- **Scope addition**: added `BaqueProcessor::set_pattern()` (immediate setter, delegates to `sequencer_.set_pattern`) so the DoD test can inject a hybrid pattern via the processor; mirrors existing `set_next_pattern`.
- **Test harness fix (not a product bug)**: `getSampleRate()` returns 0 when `prepareToPlay` is called directly in a test; a real host calls `setRateAndBufferSizeDetails` first. Test helper `prepare()` now calls `setRateAndBufferSizeDetails(48000, 512)` before `prepareToPlay`. Without it sr=0 → sequencer treats the block as infinite ppq width.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/09-midi-hardware/09-04-PLAN.md` | Current plan (audit-upgraded) |
| `.paul/phases/09-midi-hardware/09-04-AUDIT.md` | Audit report |
| `src/audio/hardware_templates.h` | TR-8/TR-8S templates (created) |
| `src/plugin_processor.{h,cpp}` | apply_hardware_template + set_pattern (modified) |
| `tests/test_hardware_templates.cpp` | Template unit tests (created, HT1-HT4) |
| `tests/test_phase9_dod.cpp` | Phase 9 DoD integration (created, P9D1-P9D6) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position.
2. The 09-04 APPLY is blocked ONLY on the Task 3 human-verify checkpoint — answer `approved` or `defer hardware` to proceed.
3. After that: `/paul:unify .paul/phases/09-midi-hardware/09-04-PLAN.md`, then the Phase 9 transition commit.
4. Uncommitted: 09-01..09-04 source + .paul/ changes, all staged for the phase-transition commit on `main`.

---

*Handoff created: 2026-06-09*
