# PAUL Handoff

**Date:** 2026-06-06
**Status:** paused mid-apply (build verification in progress)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox VST3/AU/Standalone
**Core value:** Producers build beats with authentic micro-timing feel — off-grid groove, lo-fi color, and controlled error as first-class features

---

## Current State

**Phase:** 6 of 13 — FX + P-locks
**Plan:** 06-01 — P-lock system infrastructure
**Loop Position:**
```
PLAN ──▶ AUDIT ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ~          ○
          [T1-T4 done; T5 (verify+commit) pending]
```

---

## What Was Done This Session

- Phase 5 complete (carried over from prior session): all 84 tests passing, DoD met
- Phase 5 → Phase 6 transition: PROJECT.md, ROADMAP.md, STATE.md, ledger.toml updated
- 06-01 PLAN written + audited (1M + 2SR applied, 1D deferred)
- 06-01 APPLY in progress:
  - **T1 ✅** `src/audio/plock_pattern.h` — PLockParam enum + PLockStep + PLockPattern + PLockEvent + PLockBatch (all stack-allocated)
  - **T1 ✅** `src/audio/fx_params.h` — FxParams struct (6 floats + RT-safe defaults)
  - **T2 ✅** `src/audio/sequencer.h` — `#include "plock_pattern.h"` + generate() extended with 2 optional params
  - **T2 ✅** `src/audio/sequencer.cpp` — p-lock emission block added after `last_step_fired_ = step;`
  - **T3 ✅** `src/plugin_processor.h` — new includes, `plock_pattern_` member, `apply_plock_batch()` declaration
  - **T3 ✅** `src/plugin_processor.cpp` — 6 APVTS FX params in layout, generate() call REPLACED (not inserted), `apply_plock_batch()` implementation
  - **T4 ✅** `tests/test_plock.cpp` — PL1-PL8 tests written
  - **T4 ✅** `tests/CMakeLists.txt` — test_plock.cpp added
  - **T5 ⏳** Build + test verification launched in background — result unknown at pause time

---

## What's In Progress

T5 verification: `cd build && cmake .. && make -j$(nproc) baque_tests`
- Build launched in background, not yet confirmed
- Expected result: 92/92 tests pass (84 prior + 8 new PL1-PL8)

---

## What's Next

**Immediate:** Confirm T5 — run `cd /mnt/d/work/baque/build && cmake .. && make -j$(nproc) baque_tests && ./tests/baque_tests`
- If 92/92 pass: run `clang-format --dry-run --Werror` on new files, then commit
- If build fails: check errors and fix

**After T5 passes:** Run clang-format check:
```bash
clang-format --dry-run --Werror \
    src/audio/plock_pattern.h \
    src/audio/fx_params.h \
    src/plugin_processor.cpp \
    src/plugin_processor.h \
    src/audio/sequencer.cpp \
    src/audio/sequencer.h \
    tests/test_plock.cpp
```

**After clang-format clean:** Commit with message like:
```
feat(06-plocks): p-lock system infrastructure (06-01)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

**After commit:** `/paul:unify 06-01` to close the loop

**After UNIFY:** Plan 06-02 (FxChain DSP — filter/reverb/delay)

---

## Key Design Decisions Made This Session

- **PLockBatch stack-allocated** — no heap, no AbstractFifo; push+pop both in processBlock() (single-threaded)
- **generate() M1 fix** — call is REPLACED (not inserted after) to prevent double-generate() bug emitting MIDI twice
- **apply_plock_batch() switch default** — has INVARIANT + WARNING comment; silently swallows new PLockParam additions without compiler warning — future devs warned in comment
- **PL6 test coverage strategy** — BaqueProcessor not instantiable in unit tests; AC-6 (APVTS params) covered implicitly by smoke test crash-on-null-deref

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/06-fx-plocks/06-01-PLAN.md` | Current plan (5 tasks, 8 ACs, 8 tests) |
| `.paul/phases/06-fx-plocks/06-01-AUDIT.md` | Audit results (1M+2SR applied) |
| `src/audio/plock_pattern.h` | PLock data types (new) |
| `src/audio/fx_params.h` | FxParams snapshot struct (new) |
| `src/audio/sequencer.cpp` | generate() with p-lock emission (modified) |
| `src/plugin_processor.cpp` | APVTS FX params + plock drain (modified) |
| `tests/test_plock.cpp` | PL1-PL8 tests (new) |

---

## Context: 06-01 Architecture

```
PLockPattern (per-step data, message thread writes, audio thread reads)
    │
    ▼
Sequencer::generate()  →  PLockBatch (stack-allocated, ≤32 events/block)
    │
    ▼
PluginProcessor::processBlock()
    ├─ Snapshot APVTS → FxParams (base values, one read per block)
    ├─ Apply PLockBatch → FxParams (per-step overrides for firing steps)
    └─ FxParams ready for FxChain::process() (Phase 6-02 — not yet wired)
```

No FX DSP yet — params exist but have no audible effect until 06-02.

---

## Resume Instructions

1. Run `cd /mnt/d/work/baque/build && cmake .. && make -j$(nproc) baque_tests`
2. Run `./tests/baque_tests` — verify 92/92 pass
3. Run clang-format check on new/modified files
4. Commit
5. `/paul:unify 06-01`

Or: `/paul:resume` for guided resume.

---

## RT-Safety Reminder

BAQUE hard constraint: zero allocation/locks/IO on audio thread.
All p-lock structures are stack-allocated inside processBlock(). Any future change that allocates on the audio thread is a regression.

---

*Handoff created: 2026-06-06*
