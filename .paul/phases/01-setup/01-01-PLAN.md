---
phase: 01-setup
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified: [CMakeLists.txt, src/plugin_processor.h, src/plugin_processor.cpp, src/plugin_editor.h, src/plugin_editor.cpp, LICENSE, NOTICE, .gitignore, .clang-format, .clang-tidy, README.md]
autonomous: true
description: "CMake + JUCE 8 plugin skeleton — VST3/AU/Standalone build and load empty"
type: Plan
about: "BAQUE"
---

<objective>
## Goal
Buildable JUCE 8 plugin skeleton: CMake project producing VST3 + Standalone (AU on macOS) that load empty in a host, with repo hygiene (GPLv3 license, lint configs, conventions).

## Purpose
Foundation for all 13 phases. ESCOPO Fase 0 first half. Nothing else can build without it.

## Output
- Root CMakeLists.txt with JUCE 8.x via FetchContent (pinned tag)
- Minimal PluginProcessor / PluginEditor pair (silence, empty UI)
- LICENSE (GPLv3), NOTICE (JUCE attribution), .gitignore, .clang-format, .clang-tidy, README.md
- Git repo initialized, first commit
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@BAQUE-ESCOPO.md (§5 architecture, §8 conventions, §10 platforms)

## Source Files
None yet (empty repo — only spec docs + mockup).
</context>

<acceptance_criteria>

## AC-1: Project builds
```gherkin
Given a clean checkout on Linux (WSL2)
When cmake -B build && cmake --build build is run
Then build succeeds and produces BAQUE Standalone binary and BAQUE.vst3 bundle
```

## AC-2: Plugin loads empty
```gherkin
Given the built VST3 artifact
When pluginval (basic level) runs against it
Then validation passes — plugin instantiates, processes silence, releases cleanly
```

## AC-3: Repo hygiene
```gherkin
Given the repo root
When inspecting files
Then LICENSE (GPLv3), NOTICE (JUCE + third-party attributions), .clang-format, .clang-tidy, .gitignore, README.md exist and first git commit is made
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: CMake project with JUCE 8 and plugin targets</name>
  <files>CMakeLists.txt, src/plugin_processor.h, src/plugin_processor.cpp, src/plugin_editor.h, src/plugin_editor.cpp</files>
  <action>
    Root CMakeLists.txt:
    - cmake_minimum_required 3.22+, project(BAQUE VERSION 0.0.1 LANGUAGES C CXX)
    - CMAKE_CXX_STANDARD 17, required
    - JUCE 8.x via FetchContent — <!-- audit-added --> pin EXACT release tag (check latest 8.0.x at apply time, write the chosen tag literally in CMakeLists + README; never a branch ref)
    - juce_add_plugin: COMPANY_NAME, BUNDLE_ID, PLUGIN_MANUFACTURER_CODE, PLUGIN_CODE, FORMATS VST3 Standalone + AU only when APPLE, PRODUCT_NAME "BAQUE", IS_SYNTH TRUE, NEEDS_MIDI_INPUT TRUE, NEEDS_MIDI_OUTPUT TRUE
    - <!-- audit-added --> Plugin identity is PERMANENT once any DAW session saves state. Lock now: BUNDLE_ID br.ufpb.lavid.baque, PLUGIN_MANUFACTURER_CODE 'Lvd0', PLUGIN_CODE 'Bqe1' (4-char, first uppercase). Document all three in README + add row to PROJECT.md Key Decisions.
    - Link juce::juce_audio_utils, juce::juce_dsp; recommended JUCE compile flags
    - <!-- audit-added --> Enable -Wall -Wextra (GCC/Clang) and /W4 (MSVC) on BAQUE targets only (not JUCE deps)
    Minimal processor/editor:
    - snake_case identifiers in English, comments in Portuguese, `_` suffix on private members (ESCOPO §8)
    - processBlock: clear buffer (silence), pass MIDI through untouched
    - getStateInformation/setStateInformation: <!-- audit-added --> serialize minimal ValueTree ("BAQUE" root, version="1" attribute) instead of empty stubs — preset schema versioning from day one (ESCOPO §6); setState tolerates missing/older version
    - Editor: solid background + "BAQUE" label, resizable
    Avoid: raw new/delete; allocations in processBlock; JUCE GPL splash disabling (keep GPLv3 compliant defaults).
  </action>
  <verify>cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -j → exit 0; ls build artifacts show Standalone binary + BAQUE.vst3</verify>
  <done>AC-1 satisfied: clean build produces VST3 + Standalone on Linux</done>
</task>

<task type="auto">
  <name>Task 2: Repo hygiene — license, lint configs, README, git init</name>
  <files>LICENSE, NOTICE, .gitignore, .clang-format, .clang-tidy, README.md</files>
  <action>
    - LICENSE: full GPLv3 text (project decision, PROJECT.md Key Decisions)
    - NOTICE: JUCE (GPLv3) attribution; placeholder section for SoundTouch fork (LGPL v2.1) and future third-party
    - .gitignore: build/, .cache/, IDE dirs, JUCE FetchContent artifacts
    - .clang-format: based on LLVM style, adjusted — 4-space indent, column limit 120, pointer alignment left
    - .clang-tidy: modernize-*, readability-*, performance-*, bugprone-*; identifier naming checks for snake_case + private member `_` suffix
    - README.md: project one-liner, build instructions (cmake commands), license note, links to BAQUE-ESCOPO.md / BAQUE-UI-SPEC.md
    - git init -b main + initial commit (specs + skeleton) <!-- audit-added: -b main mandatory — CI workflow (01-02) triggers on main; default master = CI silently never runs -->
  </action>
  <verify>git log shows initial commit; clang-format --dry-run src/*.cpp exits 0</verify>
  <done>AC-3 satisfied: all hygiene files present, first commit made</done>
</task>

<task type="auto">
  <name>Task 3: pluginval basic validation</name>
  <files>scripts/run_pluginval.sh</files>
  <action>
    - Download pluginval Linux release binary into build tooling dir (not committed; script fetches if absent)
    - <!-- audit-added --> Pin pluginval version in the script (PLUGINVAL_VERSION var) and verify sha256 of downloaded archive against a recorded checksum — no floating "latest"
    - scripts/run_pluginval.sh: runs pluginval --strictness-level 5 against built BAQUE.vst3; use xvfb-run if no display
    - Run it; fix any validation failures in processor (typical: state recall, bus layout)
  </action>
  <verify>./scripts/run_pluginval.sh exits 0, output shows ALL TESTS PASSED</verify>
  <done>AC-2 satisfied: pluginval basic passes on VST3</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- BAQUE-ESCOPO.md, BAQUE-UI-SPEC.md, BAQUE_MOCKUP.html (reference docs, read-only)
- .paul/* (managed by PAUL workflows)

## SCOPE LIMITS
- No DSP beyond silence — no voices, no sequencer
- No Catch2 / CI yet (plan 01-02)
- No APVTS parameters yet (Phase 2)
- No UI beyond placeholder label
- macOS/Windows builds verified in 01-02 via CI, not here

</boundaries>

<verification>
Before declaring plan complete:
- [ ] cmake -B build && cmake --build build -j exits 0 on Linux
- [ ] Standalone + VST3 artifacts exist
- [ ] ./scripts/run_pluginval.sh passes (strictness 5)
- [ ] clang-format dry-run clean on src/
- [ ] git log shows initial commit
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- No build warnings at -Wall -Wextra on BAQUE targets <!-- audit-added -->
- Plugin identity (bundle ID + 4-char codes) + exact JUCE tag documented in README and PROJECT.md <!-- audit-added -->
- Skeleton follows ESCOPO §8 conventions (snake_case, PT comments, `_` private suffix)
</success_criteria>

<output>
After completion, create `.paul/phases/01-setup/01-01-SUMMARY.md`
</output>
