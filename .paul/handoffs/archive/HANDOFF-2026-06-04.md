# PAUL Handoff

**Date:** 2026-06-04
**Status:** paused (user choice, after audit, before APPLY)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox audio plugin (C++17 / JUCE 8.x / CMake, GPLv3). Samples internally (MPC/SP style) + drives external MIDI hardware (TR-8/TR-8S). Core identity: micro-timing / off-grid feel as first-class feature.
**Core value:** Producers build beats with authentic feel of a specific lineage (Dilla, Burial, Aphex, FlyLo, Madlib, Alchemist, Bonobo, Teebs) — micro-timing, lo-fi coloration, hybrid sample+MIDI in one RT-safe instrument.

---

## Current State

**Version:** 0.0.0 (no code yet — repo holds only spec docs + mockup + .paul/)
**Phase:** 1 of 13 — Setup (ESCOPO Fase 0)
**Plan:** 01-01 + 01-02 — created + enterprise-audited, awaiting approval for APPLY

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○
```

---

## What Was Done

- PAUL initialized from BAQUE-ESCOPO.md + BAQUE-UI-SPEC.md (walkthrough skipped — specs richer)
- PROJECT.md / ROADMAP.md (13 phases + R&D-TS parallel track) / STATE.md / paul.toml / config.md populated
- Decision made: **Linux is full v1 target** (resolves ESCOPO §14.9) — 3-OS CI from start
- Plans created: 01-01 (CMake+JUCE skeleton, repo hygiene, pluginval) and 01-02 (Catch2, 3-OS CI, checkpoint for GitHub push)
- Enterprise audit run on both; upgrades auto-applied:
  - Plugin identity LOCKED: bundle `br.ufpb.lavid.baque`, manufacturer `'Lvd0'`, plugin code `'Bqe1'`
  - JUCE exact tag pin, `git init -b main`, versioned ValueTree state, -Wall -Wextra, pluginval sha256
  - CI: auval for AU, lipo universal gate, least-privilege token, xvfb-run for Linux tests
- Audit reports: 01-01-AUDIT.md, 01-02-AUDIT.md

---

## What's In Progress

Nothing mid-flight. Clean pause point: plans approved-ready, zero code written.

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/01-setup/01-01-PLAN.md` — builds CMake+JUCE skeleton on this machine (WSL2 Linux)

**After that:** `/paul:apply` 01-02 (Catch2 + CI) — contains checkpoint: user must create GitHub repo, push, confirm "ci green"

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | 13-phase overview + R&D-TS track |
| `.paul/phases/01-setup/01-01-PLAN.md` | Next plan to execute (audited) |
| `.paul/phases/01-setup/01-02-PLAN.md` | Follows 01-01 (audited, has human checkpoint) |
| `BAQUE-ESCOPO.md` | Scope source of truth (PT) |
| `BAQUE-UI-SPEC.md` / `BAQUE_MOCKUP.html` | UI spec + visual source of truth |

---

## Notes for Fresh Session

- No git repo yet — `git init -b main` is task 2 of plan 01-01
- Machine = WSL2; Linux build/pluginval (xvfb) work here; macOS/Windows verified via CI in 01-02
- Open ESCOPO decisions still pending: sample embed (#5, revisit Phase 11), song mode depth (#6, Phase 3), multi-out (#7, Phase 9/10)
- Audit deferred items: WSL2 /mnt/d build perf, branch protection, state round-trip test, clang-tidy CI job

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Check loop position
3. Run `/paul:resume` or `/paul:progress`

---

*Handoff created: 2026-06-04*
