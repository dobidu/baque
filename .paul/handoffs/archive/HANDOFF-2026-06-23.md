# PAUL Handoff

**Date:** 2026-06-23
**Status:** paused — mid-APPLY, GHA release loop iterating

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox audio plugin (VST3/AU/Standalone, C++17/JUCE 8/CMake, GPLv3)
**Core value:** Producers build beats with authentic micro-timing feel — off-grid groove, lo-fi color, controlled error as first-class features

---

## Current State

**Version:** 1.0.0 (bumped from 0.0.1 in Plan 13-01)
**Phase:** 13 of 13 — Release
**Plan:** 13-02 — CPack + release.yml + codesign + v1.0.0 tag + Phase 13 DoD

**Loop Position:**
```
PLAN ──▶ AUDIT ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○ (in progress)    ○
```

---

## What Was Done This Session

### Plan 13-01 (completed, loop closed)
- README.md rewritten: 88 → 176 lines, 9 sections, per-platform VST3 install paths
- NOTICE updated: LGPL §6 compliance with actual fork URL (https://github.com/dobidu/baque-soundtouch, tag v1.0.0-baque) — placeholder removed
- CMakeLists.txt: `project(BAQUE VERSION 1.0.0)` (was 0.0.1)
- CMakeLists.txt: `install()` rules for VST3 + Standalone (BAQUE.app dir on macOS) + AU
- 13-01-AUDIT.md, 13-01-SUMMARY.md created; loop closed

### Plan 13-02 APPLY (in progress — iterating on GHA)
Committed to main:
- `feat(13)`: CPack config in CMakeLists.txt + `.github/workflows/release.yml` (3-platform matrix + release job)
- `fix(13)`: YAML block scalar bug in release notes (blank lines at col 0 terminated run:| scalar)
- `fix(13)`: secrets context not available in step if: → job-level `env: APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}` + `if: env.APPLE_SIGNING_IDENTITY != ''`
- `fix(13)`: Windows — `dlfcn.h` guarded behind `#ifndef _WIN32`, `dl` link via generator expression
- `fix(13)`: macOS — `free(void* p) noexcept` → `free(void* p)` (Apple Clang exception spec mismatch)
- `style`: clang-format `preset_manager.h` + `rt_safety.h` (CI lint fix)
- `fix(13)`: macOS CPack — `install(DIRECTORY .../BAQUE.component)` has no OPTIONAL flag; replaced with `install(CODE)` + `if(EXISTS)` guard
- `fix(13)`: release job `gh release create` needs `--repo github.repository` (no checkout in release job → "not a git repository")

### CPack local verification (Linux)
- `BAQUE-1.0.0-Linux.zip` = 16 MB, 9 files (VST3/BAQUE.vst3 + Standalone/BAQUE), 0 JUCE headers
- Fix required: `CPACK_ARCHIVE_COMPONENT_INSTALL ON` + `CPACK_COMPONENTS_GROUPING ALL_COMPONENTS_IN_ONE` + `CMAKE_INSTALL_DEFAULT_COMPONENT_NAME "ExternalDeps"` for JUCE/SoundTouch deps

---

## What's In Progress

**GHA release workflow** — run `28000487842` just queued (triggered by `git push origin v1.0.0`).

Previous run `27993333320` had:
- Linux ✓ (6m57s), Windows ✓ (8m22s), macOS ✓ (1h49m14s) — all 3 builds PASSED
- Create GitHub Release ✗ — `fatal: not a git repository` (missing `--repo` flag)
- Artifacts uploaded: BAQUE-Linux, BAQUE-Windows, BAQUE-macOS

Current run `28000487842` has the `--repo` fix. **macOS will take ~2h** (JUCE universal cold build). Linux/Windows will finish in ~10 min.

---

## What's Next

**Immediate:** Wait for run `28000487842` to complete. Check with:
```bash
gh run view 28000487842
```

**If Create GitHub Release succeeds:**
1. Verify release at https://github.com/dobidu/baque/releases/tag/v1.0.0
2. Confirm 3 ZIPs: BAQUE-1.0.0-Linux.zip, BAQUE-1.0.0-macOS.zip, BAQUE-1.0.0-Windows.zip
3. Run `/paul:unify` for Plan 13-02 → loop closes → Phase 13 DoD → Milestone v1.0 COMPLETE

**If Create GitHub Release fails again:**
- Check: `gh run view --job=<release-job-id> --log`
- Previous error was `fatal: not a git repository` → fixed with `--repo`
- Possible next issue: notes file `/tmp/release-notes.md` path or heredoc content

**Parallel (CI lint):** CI run will lint `rt_alloc_counter.cpp` (clang-tidy warnings, not errors). These are pre-existing warnings in the `#else` POSIX block — not blocking CI green.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/13-release/13-02-PLAN.md` | Current plan |
| `.github/workflows/release.yml` | Release workflow — the thing being iterated |
| `CMakeLists.txt` | CPack config + install() rules (end of file) |
| `tests/rt_alloc_counter.cpp` | POSIX malloc interception — `#ifdef _WIN32` guard added |

---

## GHA Run History (this session)

| Run | Result | What failed |
|-----|--------|-------------|
| 27862814078 | ✗ (0s) | YAML parse error — `**Install:**` at col 0 terminated block scalar |
| 27862867354 | ✗ (0s) | Same |
| 27863159048 | ✗ (0s) | Same + secrets in if: condition (GHA validation) |
| 27863213803 | ✗ (0s) | secrets in if: condition |
| 27863697675 | ✗ | Windows: `dlfcn.h` not found; macOS: building (cancelled) |
| 27864399007 | ✗ | Windows ✓, Linux ✓; macOS: `free() noexcept` exception spec mismatch |
| 27936604146 | ✗ | macOS CPack: `BAQUE.component` not found (no OPTIONAL on install(DIRECTORY)) |
| 27993333320 | ✗ | Linux ✓, Windows ✓, macOS ✓; Release job: `fatal: not a git repository` (no --repo) |
| **28000487842** | **queued** | **--repo fix applied — expected to succeed** |

---

## Resume Instructions

1. Check run `28000487842`: `gh run view 28000487842`
2. If succeeded: verify release at https://github.com/dobidu/baque/releases/tag/v1.0.0
3. Run `/paul:unify` to close 13-02 loop
4. If failed: check release job log, fix, re-tag v1.0.0

---

*Handoff created: 2026-06-23*
