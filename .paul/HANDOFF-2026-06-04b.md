# PAUL Handoff

**Date:** 2026-06-04 (session 2)
**Status:** paused — 01-01 APPLY complete, awaiting UNIFY before 01-02

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox audio plugin (C++17 / JUCE 8.0.13 / CMake, GPLv3). Samples internally (MPC/SP style) + drives external MIDI hardware (TR-8/TR-8S). Core identity: micro-timing / off-grid feel as first-class feature.
**Core value:** Producers build beats with authentic feel of a specific lineage (Dilla, Burial, Aphex, FlyLo, Madlib, Alchemist, Bonobo, Teebs) — micro-timing, lo-fi coloration, hybrid sample+MIDI in one RT-safe instrument.

---

## Current State

**Version:** 0.0.0
**Phase:** 1 of 13 — Setup (ESCOPO Fase 0)
**Plan:** 01-01 APPLY complete; 01-02 not started

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○
```

---

## What Was Done This Session

- `/paul:resume` restored context from prior handoff
- `/paul:apply 01-01` executed all 3 tasks:
  - **Task 1:** CMakeLists.txt with JUCE 8.0.13 (FetchContent, pinned), BaqueProcessor + BaqueEditor skeleton — builds clean on Linux, Standalone + VST3 artifacts produced
  - **Task 2:** LICENSE (GPLv3), NOTICE (JUCE + SoundTouch placeholder), .gitignore, .clang-format, .clang-tidy, README.md — git init -b main, initial commit
  - **Task 3:** scripts/run_pluginval.sh (pinned v1.0.4, sha256 verified) — pluginval strictness 5: ALL TESTS PASSED
  - GAP caught + fixed: source files had clang-format violations → auto-fixed, amended commit
- Git history: 2 commits on main (skeleton + hygiene, then pluginval script)

---

## What's In Progress

Nothing mid-flight. APPLY done, UNIFY not yet run.

---

## What's Next

**Immediate:** `/paul:unify .paul/phases/01-setup/01-01-PLAN.md` — close the loop, update PROJECT.md Validated section

**After that:** `/paul:apply .paul/phases/01-setup/01-02-PLAN.md` — Catch2 + 3-OS CI. Contains checkpoint: user creates GitHub repo, pushes, confirms "ci green"

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | 13-phase overview |
| `.paul/phases/01-setup/01-01-PLAN.md` | Completed plan (awaiting UNIFY) |
| `.paul/phases/01-setup/01-02-PLAN.md` | Next plan after UNIFY |
| `CMakeLists.txt` | Build entry point (JUCE 8.0.13 pinned) |
| `scripts/run_pluginval.sh` | pluginval v1.0.4 (pinned + sha256) |

---

## Notes for Fresh Session

- build/ is gitignored and present locally; no rebuild needed on this machine
- scripts/pluginval binary also gitignored and present locally
- Plugin identity: br.ufpb.lavid.baque / 'Lvd0' / 'Bqe1' — locked, do not change
- 01-02 audit already done (01-02-AUDIT.md): AU validation (auval), lipo universal gate, CI least-privilege, xvfb-run, concurrency cancel all required

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress`

---

*Handoff created: 2026-06-04 (session 2)*
