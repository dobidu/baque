# PAUL Handoff

**Date:** 2026-06-10 (session 24)
**Status:** paused — clean phase boundary (Phase 9 complete, Phase 10 not started)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BAQUE — hybrid groovebox audio plugin (VST3/AU/Standalone, JUCE 8 / C++17, GPLv3).
**Core value:** Producers build beats with authentic micro-timing feel — off-grid groove, lo-fi color, controlled error as first-class features.

---

## Current State

**Version:** 0.0.0
**Phase:** 9 of 13 — MIDI / Hardware — **COMPLETE** → next is Phase 10 (UI/UX)
**Plan:** none active (ready to plan Phase 10)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Phase 9 closed + transitioned]
```

**Milestone v1.0:** 9/13 phases complete (69%).

---

## What Was Done (this session)

- Resumed at 09-04 APPLY (paused on Task 3 real-TR-8 human-verify checkpoint, no hardware on this box).
- Instead of deferring hardware, built **Tier-3 spec conformance** (no hardware needed):
  - `src/audio/tr8_midi_spec.h` — authoritative constants from Roland's published **TR-8 MIDI Implementation Chart (v1.11)** (note numbers + CC numbers; source URL in header). Independent of the template so the test is a real equality check.
  - `tests/test_tr8_spec_conformance.cpp` (TS1-TS5) — asserts the hand-written template matches the chart. Note map **spec-verified** (every note = chart primary).
  - Baked **value-safe continuous CCs** into `tr8_template()` from the chart: scatter_depth=69, reverb_mix=91, delay_mix=16, delay_time=17, `cc_out_enabled=true`. `scatter_type=68` known but left **OFF** (discrete pattern-select value-curve needs a physical unit).
  - Fixed `P9D5`: was CC 16 for scatter_depth — 16 is DELAY LEVEL per chart; corrected to 69. Updated HT1/HT2 for the CC-on state.
- **AC-6 hardware sign-off resolved verified-by-spec** (the published chart IS the firmware spec for note/CC numbers → byte mapping confirmed without hardware).
- `/paul:unify 09-04` → SUMMARY written, STATE/PROJECT/ROADMAP/paul.toml/ledger updated, handoff archived.
- **Phase 9 transition commit:** `c6792d7 feat(09): Phase 9 complete — MIDI / Hardware` (24 files, 09-03 + 09-04 source + .paul).
- **209/209 tests pass.**

---

## What's In Progress

- Nothing mid-flight. Clean boundary.
- Tiny uncommitted tail: a 1-line STATE.md edit (the git-hash line, written after the commit) + untracked `.claude/` (framework/skills config, intentionally not tracked).

---

## What's Next

**Immediate:** `/paul:plan` for **Phase 10 — UI/UX** (LookAndFeel, all 6 screens, visualizers, undo/redo; BAQUE-UI-SPEC.md + BAQUE_MOCKUP.html are the visual source of truth).

**Phase 10 entry constraint (carry-over):** `lane_routing_`, `cc_out_`, `clock_master_`, hardware-template apply, PerfState, and pad-params are all **single-writer setup structs** — the UI MUST atomicize / use a command queue before any live edits. This is the recurring contract flagged across Phases 4/8/9.

**Time-critical, separate:** CI `actions/checkout`+`cache` v4→v5 on branch `ci/node20-actions-v5`, **PR #1** (https://github.com/dobidu/baque/pull/1) — STILL OPEN, **merge before June 16** (Node.js 20 deprecation; ~6 days).

---

## Open Hardware Residuals (deferred, no CI gate)

- TR-8 `scatter_type` CC68 value-curve (BAQUE 0-10 types vs TR-8 0-127 select) — number known, slot OFF until a real unit confirms the mapping.
- Physical clock-jitter measurement; TR-8S assignable routing + TR-8S-only CC banks; hardware template import/export file format (→ Phase 11 presets).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (Phase 10 is next) |
| `.paul/PROJECT.md` | Requirements + decisions (evolved through Phase 9) |
| `.paul/phases/09-midi-hardware/09-04-SUMMARY.md` | Last plan summary |
| `src/audio/hardware_templates.h` | TR-8/TR-8S templates (value-safe CCs baked) |
| `src/audio/tr8_midi_spec.h` | Roland chart source-of-truth constants |
| `BAQUE-UI-SPEC.md` / `BAQUE_MOCKUP.html` | Phase 10 visual source of truth |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position.
2. Loop is clean at a phase boundary — no checkpoint pending.
3. Run `/paul:resume` (or `/paul:progress`), then `/paul:plan` for Phase 10.

---

*Handoff created: 2026-06-10*
