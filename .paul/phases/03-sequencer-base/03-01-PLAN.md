---
phase: 03-sequencer-base
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified: [src/audio/step_pattern.h, src/audio/step_pattern.cpp, src/audio/step_clock.h, src/audio/step_clock.cpp, src/audio/sequencer.h, src/audio/sequencer.cpp, src/plugin_processor.h, src/plugin_processor.cpp, CMakeLists.txt, tests/test_sequencer.cpp]
autonomous: true
description: "StepPattern data model + StepClock (ppq→step+sample) + sequencer generates MidiBuffer fed to existing Scheduler"
type: Plan
about: "BAQUE"
---

<objective>
## Goal
Implement a 16-step sequencer: StepPattern (per-lane step grid, 16 steps × 16 lanes), StepClock (converts ppq_position + bpm → current step + sample-accurate offset), and Sequencer (generates MidiBuffer events for active steps, fed to the existing Scheduler::process() from Phase 2).

## Purpose
First repeating pattern. Everything in Phase 3+ is built on this timing foundation. ESCOPO §4.2 sequencer base. The Scheduler already handles sample-accurate dispatch — the sequencer only needs to generate the right MidiBuffer.

## Output
- `src/audio/step_pattern.h/.cpp` — StepPattern: 16 lanes × 16 steps, per-step on/off + MIDI note mapping
- `src/audio/step_clock.h/.cpp` — StepClock: converts host ppq_position to step number + sub-step sample offset; handles loop-around; RT-safe
- `src/audio/sequencer.h/.cpp` — Sequencer: owns StepPattern + StepClock; called from processBlock; populates MidiBuffer
- `src/plugin_processor.h/.cpp` — add Sequencer member; processBlock calls sequencer before Scheduler
- `tests/test_sequencer.cpp` — unit tests for StepClock timing + StepPattern step queries
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/02-core-audio/02-02-SUMMARY.md (Scheduler::process() + TransportState — Sequencer feeds into these)

## Source Files
@src/audio/scheduler.h
@src/plugin_processor.h
@src/plugin_processor.cpp
</context>

<acceptance_criteria>

## AC-1: Pattern loops at correct tempo
```gherkin
Given a 16-step pattern with steps 1, 5, 9, 13 on (quarter-note kick)
When the sequencer runs with host playing at 120 BPM
Then a note-on event fires every beat (4 steps × 500ms = every 500ms at 120 BPM) without drift
```

## AC-2: Steps fire at sample-accurate positions
```gherkin
Given a pattern with step N active
When the sequencer computes MidiBuffer events for a processBlock call
Then the note-on event's samplePosition matches the sample within the block where the step boundary falls (computed from ppq_position + bpm + sample rate)
```

## AC-3: Note-off tracks active notes per step
```gherkin
Given a step fires a note-on in block B
When the step ends (next step boundary in block B+1 or later)
Then a note-off event fires at the correct samplePosition so no notes are stuck
```

## AC-4: Sequencer tests pass
```gherkin
Given the test suite
When ctest runs
Then all new StepClock + StepPattern tests pass alongside existing 11 tests
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: StepPattern + StepClock data structures</name>
  <files>src/audio/step_pattern.h, src/audio/step_pattern.cpp, src/audio/step_clock.h, src/audio/step_clock.cpp</files>
  <action>
    **StepPattern** (`src/audio/step_pattern.h/.cpp`):
    - `static constexpr int k_num_steps = 16;`
    - `static constexpr int k_num_lanes = 16;`
    - Per-lane per-step: `struct Step { bool active = false; uint8_t note = 36; };` (note 36 = C2 = kick by convention)
    - `std::array<std::array<Step, k_num_steps>, k_num_lanes> steps_` — fixed-size, no allocation
    - Methods: `is_active(int lane, int step) const noexcept`, `get_note(int lane, int step) const noexcept`, `set_active(int lane, int step, bool val) noexcept`
    - Default pattern: steps 0, 4, 8, 12 active on lane 0 (quarter-note kick) — produces audible output immediately
    ESCOPO §8 conventions.

    **StepClock** (`src/audio/step_clock.h/.cpp`):
    - Converts ppq_position → step number (0–15) at 1/16th note resolution.
    - 1/16th note = 0.25 ppq. Step N fires when ppq crosses N × 0.25.
    - `int current_step(double ppq_position) const noexcept` — returns step index (0–15), no state
    - `int samples_until_next_step(double ppq_position, double bpm, double sample_rate, int block_size) const noexcept` — returns sample offset within current block where NEXT step boundary falls; returns `block_size` if boundary is outside block
    - `int sample_offset_of_step_start(double ppq_position_at_block_start, double bpm, double sample_rate, int step_target) const noexcept` — returns sample offset within a block where a specific step begins (used to compute samplePosition for MidiBuffer)
    - Stateless: all inputs passed explicitly — RT-safe, no members to protect
    - Handles ppq loop-around (end of 4-bar pattern → back to 0) gracefully
    Avoid: floating-point rounding that causes double-triggers at step boundaries (use `>= N*0.25` with small epsilon guard).
  </action>
  <verify>Unit tests in test_sequencer.cpp (Task 3) compile and pass</verify>
  <done>AC-2 partial: StepClock correctly computes step from ppq_position</done>
</task>

<task type="auto">
  <name>Task 2: Sequencer + processor wiring</name>
  <files>src/audio/sequencer.h, src/audio/sequencer.cpp, src/plugin_processor.h, src/plugin_processor.cpp, CMakeLists.txt</files>
  <action>
    **Sequencer** (`src/audio/sequencer.h/.cpp`):
    - Members: `StepPattern pattern_`, `StepClock clock_`
    - Method: `void generate(const TransportState& transport, juce::MidiBuffer& midi_out, int block_size, double sample_rate) noexcept`
      - If !transport.is_playing: return (no events)
      - Determine which step boundaries fall within this block using clock_
      - For each step boundary within block:
        - Compute samplePosition of boundary (sample offset within block)
        - For each active lane at that step: add note-on to midi_out at samplePosition
        - For the PREVIOUS step's active lanes: add note-off at samplePosition (prevents stuck notes — AC-3)
          - Gate length = full step (note-off at next step boundary)
      - No allocation; midi_out is cleared by caller before generate()
    - `StepPattern& pattern() noexcept { return pattern_; }` — accessor for UI/future binding

    **plugin_processor.h** additions:
    - `#include "audio/sequencer.h"`
    - `Sequencer sequencer_` member

    **plugin_processor.cpp** changes in processBlock:
    - Before `scheduler_.process(...)`: call `sequencer_.generate(transport_, midi_buffer_seq_, num_frames, getSampleRate())`
    - `midi_buffer_seq_` is a pre-allocated `juce::MidiBuffer` member (allocated in prepareToPlay, not processBlock)
    - Merge midi_messages (external MIDI in) and midi_buffer_seq_ (sequencer) before passing to Scheduler
    - Actually: call scheduler_.process() twice — once with sequencer buffer, once with external MIDI
      - OR: merge into a local buffer (simpler; MidiBuffer has addEvents())

    **CMakeLists.txt**: add `src/audio/sequencer.cpp` to target_sources.

    Avoid: any allocation in processBlock; juce::MidiBuffer::addEvents is RT-safe (no alloc if pre-allocated).
  </action>
  <verify>cmake --build build -j exits 0; BAQUE Standalone runs (no crash); ctest passes</verify>
  <done>AC-1 + AC-3 satisfied: pattern loops at correct tempo; note-off prevents stuck notes</done>
</task>

<task type="auto">
  <name>Task 3: Sequencer unit tests</name>
  <files>tests/test_sequencer.cpp, tests/CMakeLists.txt</files>
  <action>
    New test file, ASCII-only names, added to tests/CMakeLists.txt.

    **Tests:**

    1. `"step clock returns correct step from ppq"`:
       - StepClock at ppq 0.0 → step 0
       - ppq 0.25 → step 1
       - ppq 0.5 → step 2
       - ppq 3.75 → step 15
       - ppq 4.0 → step 0 (loop-around)
       - ppq 0.24999 → step 0 (below boundary)

    2. `"step clock sample offset within block"`:
       - At 120 BPM, 44100Hz, 1/16th = 0.25 ppq = 0.125s = 5512.5 samples
       - Given ppq_at_block_start = 0.0, block_size = 512, bpm = 120, sr = 44100
       - Next step boundary (ppq 0.25) is 5512 samples away → `block_size` returned (not within block)
       - Given ppq_at_block_start = 0.249 (just before step 1), bpm = 120, sr = 44100
       - Step 1 boundary falls within small number of samples → sample_offset_of_step_start < block_size

    3. `"step pattern default has kick on beats"`:
       - Create StepPattern; assert steps 0, 4, 8, 12 active on lane 0
       - Assert steps 1, 2, 3 inactive on lane 0

    4. `"step pattern set and get"`:
       - set_active(0, 7, true); REQUIRE(is_active(0, 7))
       - set_active(0, 7, false); REQUIRE(!is_active(0, 7))

    5. `"sequencer generates no events when not playing"`:
       - TransportState with is_playing=false
       - sequencer.generate(transport, midi, 512, 44100)
       - REQUIRE(midi.isEmpty())

    6. `"sequencer generates note-on at step boundary"`:
       - TransportState is_playing=true, bpm=120, ppq_position just before step 1 boundary
       - generate() for 512-frame block
       - REQUIRE midi contains at least one note-on event
       - samplePosition of event within [0, 512)
  </action>
  <verify>ctest --test-dir build --output-on-failure — all tests pass (6 new + 11 existing = 17 total)</verify>
  <done>AC-4 satisfied: all sequencer tests pass</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/audio/scheduler.h/.cpp (Sequencer feeds Scheduler, does not modify it)
- src/audio/voice_pool.h/.cpp (stable from Phase 2)
- src/audio/sample_voice.h/.cpp (stable from Phase 2)
- src/plugin_editor.h/.cpp
- tests/test_smoke.cpp, tests/test_voice.cpp, tests/test_transport.cpp

## SCOPE LIMITS
- No swing yet (Plan 03-02)
- No pattern switching yet (Plan 03-02)
- No per-step velocity, probability, ratchets (Phase 5+ / ESCOPO §4.2)
- No polymeter (different lane lengths) — Phase 5
- No Elektron trig conditions — Phase 5
- No p-locks — Phase 6
- No UI for step grid — Phase 10
- 16 steps fixed (extendable in Phase 3+, not now)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] cmake --build build -j exits 0
- [ ] ctest: all 17 tests pass (6 new + 11 existing)
- [ ] Default pattern (kick on beats 1/2/3/4) fires correctly at 120 BPM — verify by running Standalone and listening/checking MIDI output
- [ ] clang-format --dry-run exits 0 on src/audio/ tests/
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- Pattern loops at correct tempo (step 0,4,8,12 every quarter-note at 120 BPM)
- Note-off fires at step boundary (no stuck notes)
- StepClock sample-accurate step detection tested
- 17/17 ctest pass
</success_criteria>

<output>
After completion, create `.paul/phases/03-sequencer-base/03-01-SUMMARY.md`
</output>
